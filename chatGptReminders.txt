Project preferences for this codebase:

- Always make code changes fully functional and production-ready.
- Do not leave TypeScript, ESLint, or React hook/compiler errors behind.
- After changes, validate with the relevant checks and tell me if anything could not be run.
- Add code comments anywhere the logic is non-obvious.
- Prefer comments that explain why something exists or how a tricky flow works, not obvious line-by-line narration.
- Continue prioritizing comments in future edits unless I say otherwise.

Draggable grid behavior preferences:

- The grid should feel natural and human-friendly, not overly strict.
- Users should be able to freely move items around the screen.
- Open spaces should be valid placement targets.
- The layout should not always collapse up and to the left.
- Dragging should use explicit placement behavior rather than only order-based dense packing.
- The drag location should feel anchored to where the user grabbed the card, not to the card’s left edge.
- Dragging over items should feel soft and exploratory.
- Other items should not immediately move just because the pointer passed over them.
- Dragging should show a temporary draft/preview layout so users can see the likely landing spot and affected items before drop.
- Drag previews should be derived from the drag-start layout snapshot rather than mutating committed layout during drag.
- The actively dragged item can be treated as a special faded placeholder during drag.
- Keep the dragged item visible in its original slot until the preview actually needs that space for another card.
- Drag previews should clear when the pointer moves on or the drag ends; only drop should commit the layout change.
- When moving between nearby drag targets, the preview should transition directly to the next target instead of animating back to the committed layout in between.
- The dotted/dashed drop preview should stay lightweight in the dragover hot path; avoid extra React state when a ref-driven marker will do.
- Preview movement should feel snappy and near-immediate, with only a very short dwell before updates appear.
- Final layout changes should commit on drop, not during transient drag-over states.
- DraggableGrid should be read-only by default unless `canEdit={true}` is passed.
- When `canEdit` is false, disable drag/drop edits, hide resize handles, hide Collapse/Optimize/Undo controls, hide the bottom row-resize handle, and disable Ctrl/Cmd+Z undo.
- When `canEdit` is true, the grid should visibly read as editable through reusable layout chrome: a lightly tinted canvas, subtle dashed item outlines, and clearer resize/tool affordances rather than relying only on cursors or buttons.
- Keep edit-mode chrome subtle and content-first; avoid heavy gradients or decorative top accent bars on cards.
- Per-card edit overlays should avoid tint washes over content or controls; prefer border-only cues if overlays start coloring demo UI like delete buttons.
- The demo should include an edit-mode toggle that drives `canEdit`; when viewing/read-only, external demo controls like Add Item should not mutate the layout.
- Starting a brand new dashboard via `New` should automatically switch the demo back into edit mode so the empty draft is immediately editable.
- The demo should support multiple named dashboards in localStorage, with controls to create a new draft, save/update the active dashboard, save a copy, delete the currently selected saved dashboard, and switch which saved dashboard is currently displayed.
- The demo storage can target only the current multi-dashboard localStorage format; no legacy single-layout migration path is needed.
- New dashboards and the default unsaved draft should start empty rather than with seeded sample cards.
- Deleting a saved dashboard from the demo should be a confirmed action; after deletion, load another saved dashboard if one exists, otherwise fall back to an empty unsaved draft.
- The demo should feel more like a simple app shell than a floating playground: use a clean toolbar above the grid for dashboard selection, naming, save actions, add-item actions, and edit mode, while keeping the dashboard canvas visually plain below it.
- Dragging should have a clear floating visual state while moving.
- Dashboard card content should stay readable at the smallest 1x1 size.
- For standard cards, prefer a simple title plus compact max-size and current-size chips over dense metadata blocks.
- In 1x1 cards, prioritize keeping the title visible; hide chips or other secondary metadata before truncating the name.
- In single-row cards, prioritize the title over metadata; 2x1 cards should hide chips before cutting off the name.
- Omitted card size fields should default sensibly: current width/height to 1, minimums to 1, and maximums to unlimited.
- The demo UI should support adding dashboard items through an Add Item menu, with templates like basic cards, buttons/actions, charts, filters, rich text notes, uploaded images, and metrics appended at the bottom of the grid.
- Filter dashboard items should be configurable inline in edit mode with a display name, URL param name, and dropdown options; selecting a filter value should update the browser URL search params without reloading the page.
- Demo chart items should randomize between line, scatter, pie, and column charts at creation time, with realistic sample data stored on the item itself so saved dashboards keep the same chart contents and multiple chart cards feel varied. Prefer random selection among the least-used chart types already on the dashboard so new charts do not cluster too heavily into one type.
- Chart cards should use sensible per-type defaults: line/column/scatter should start larger (around 4x3 with practical minimums), while pie charts can stay more compact; chart previews should use the available card area well and column charts should read like conventional bar/column charts rather than decorative pills.
- Chart card headers should vertically center the title text with the colored chart badge so the title row feels aligned and tidy.
- Pie chart cards should center their chart layout and let the donut use more of the available space instead of hugging one side or reading too small.
- Pie chart previews should stay strictly contained within the card; on compact chart tiles, prioritize the donut itself and hide the legend rather than letting the chart spill outside the card bounds.
- On an empty dashboard, the first added item should start at row 1, column 1; only later additions should append below the current layout footprint.
- Deleting cards is a demo-level behavior, not part of the `DraggableGrid` library API; demo cards can expose their own delete affordance that opts out of drag gestures and removes the item from demo layout state.
- Demo-only card controls like delete buttons should be positioned relative to the card itself so they naturally move with card hover/drag animations instead of needing their own sync transforms.
- Demo delete buttons should keep a solid white background on hover so card-hover styling does not muddy the control.
- Rich text dashboard items should use an actual editable rich text surface in edit mode, save their HTML output to the item body, and render the saved output in viewing mode.
- Rich text edit/view mode should preserve the same sanitized output, including safe inline styles for color, highlight, font size, font family, alignment, line height, and text decorations.
- Rich text color/highlight controls should preserve editor selection; prefer toolbar swatches/custom prompts over native color inputs that steal focus before formatting runs.
- Text editing surfaces inside cards should opt out of grid drag gestures and grid-level Ctrl/Cmd+Z so typing, selecting, formatting, and native editor undo feel normal.
- React 19 click-to-source tooling should not rely on `click-to-react-component`/`_debugSource`; use dev-only JSX source metadata injection plus `click-to-component-browser` (or the matching browser extension) so Option+Click/Option+RightClick can still open VS Code from Vite.

Resize/grid preferences:

- Item resize-handle interactions should feel intentional and easy to understand.
- The item resize handle should stay visually inside the card.
- Hover/lift animations should move the resize handle with the card so it feels attached.
- Any edit-mode overlays or affordances, like dashed editable borders, should move with the same hover/lift animation as the card so they feel attached rather than layered on top.
- The resize handle style should be modern and subtle.
- Resizing across multiple widths/heights should stay smooth and should not jitter or snap.
- Width/height changes during resize should remain visibly animated rather than jumping instantly.
- Neighboring cards should reposition smoothly during item resize.
- The overall grid should support a resizable number of rows with visible empty space.
- A bottom handle for increasing/decreasing total grid rows is desired.
- Empty rows/space should be preserved as part of the layout model.
- Debug gridlines should be a grid item spanning the actual card grid tracks so browser layout accounts for padding, row height, and gap instead of relying on manual overlay offsets or percentage approximations.

Undo/history preferences:

- Support Ctrl+Z / Cmd+Z undo.
- Also provide an Undo button in the UI.
- Also provide Collapse and Optimize buttons beside Undo for layout compaction.
- Undo history should only record finalized interactions with a real diff.
- Drag preview/hover states should not be added to undo history.
- Drop commits should be added to history.
- Drag-handle resize commits with a real diff should also be added to history.
- Finalized external layout changes, like adding a card via UI controls, should also be undoable.
- Collapse/compact should count as a finalized undoable commit.
- Collapse should determine item order by scanning rows left-to-right, then top-to-bottom, commit that same order, and place each item as high as possible before moving left. Later items must respect the full footprint of earlier ordered items: while they are in the same vertical band, they should stay to the right rather than jumping left/up around them.
- Optimize should count as a finalized undoable commit and may reorder cards to pack larger items first for a denser layout.

General collaboration preferences:

- Treat this file as the evolving knowledge base for the project.
- Any durable code, UX, interaction, architecture, or workflow updates made during collaboration should be reflected here.
- At the start of a new conversation, this file should be treated as the baseline project context and source of truth for established preferences/decisions.
- If something seems worth explaining, add a code comment.
- When a behavior is tricky, prefer clear structure and readability over cleverness.
- If a design/interaction model is fundamentally fighting the desired UX, prefer rethinking the model instead of patching it repeatedly.
- In drag/resize hot paths, prefer memoized derived data and stable DOM ref callbacks over duplicated React state or per-render callback churn.
- When a file starts accumulating pure helper logic or presentational subviews, extract them into focused modules/components instead of letting the primary file become a catch-all.
